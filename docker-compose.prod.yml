services:
  caddy:
    image: caddy:2.8-alpine
    container_name: talentup-caddy
    restart: unless-stopped
    depends_on:
      - frontend
      - backend
    ports:
      - "80:80"
      - "443:443"
    environment:
      APP_DOMAIN: ${APP_DOMAIN}
      LETSENCRYPT_EMAIL: ${LETSENCRYPT_EMAIL}
    volumes:
      - ./infra/Caddyfile.example:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        VITE_API_URL: ${VITE_API_URL}
    container_name: talentup-frontend
    restart: unless-stopped
    expose:
      - "80"

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: talentup-backend
    restart: unless-stopped
    env_file:
      - .env.prod
    depends_on:
      redis:
        condition: service_healthy
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      REDIS_URL: redis://redis:6379/0
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      VACANCY_DIGEST_INTERVAL_MINUTES: ${VACANCY_DIGEST_INTERVAL_MINUTES:-10080}
      VACANCY_DIGEST_DISPATCH_INTERVAL_SECONDS: ${VACANCY_DIGEST_DISPATCH_INTERVAL_SECONDS:-1800}
      FRONTEND_ORIGIN: ${FRONTEND_ORIGIN}
      RESUME_UPLOAD_DIR: /app/backend/uploads/resumes
      MAX_PDF_SIZE_BYTES: ${MAX_PDF_SIZE_BYTES:-5242880}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      OAUTH_SESSION_SECRET: ${OAUTH_SESSION_SECRET}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL}
      SMTP_USE_TLS: ${SMTP_USE_TLS:-true}
    expose:
      - "8000"
    volumes:
      - backend_uploads:/app/backend/uploads
    command: >
      sh -c "alembic upgrade head &&
      uvicorn backend.main:app --host 0.0.0.0 --port 8000"

  celery_worker:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: talentup-celery-worker
    restart: unless-stopped
    env_file:
      - .env.prod
    depends_on:
      redis:
        condition: service_healthy
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      REDIS_URL: redis://redis:6379/0
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      VACANCY_DIGEST_INTERVAL_MINUTES: ${VACANCY_DIGEST_INTERVAL_MINUTES:-10080}
      VACANCY_DIGEST_DISPATCH_INTERVAL_SECONDS: ${VACANCY_DIGEST_DISPATCH_INTERVAL_SECONDS:-1800}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL}
      SMTP_USE_TLS: ${SMTP_USE_TLS:-true}
    volumes:
      - backend_uploads:/app/backend/uploads
    command: >
      celery -A worker.messages.celery_app.celery_app worker --pool=solo --loglevel=INFO

  celery_beat:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: talentup-celery-beat
    restart: unless-stopped
    env_file:
      - .env.prod
    depends_on:
      redis:
        condition: service_healthy
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      REDIS_URL: redis://redis:6379/0
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      VACANCY_DIGEST_INTERVAL_MINUTES: ${VACANCY_DIGEST_INTERVAL_MINUTES:-10080}
      VACANCY_DIGEST_DISPATCH_INTERVAL_SECONDS: ${VACANCY_DIGEST_DISPATCH_INTERVAL_SECONDS:-1800}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL}
      SMTP_USE_TLS: ${SMTP_USE_TLS:-true}
    volumes:
      - backend_uploads:/app/backend/uploads
    command: >
      celery -A worker.messages.celery_app.celery_app beat --loglevel=INFO --schedule=/tmp/celerybeat-schedule

  redis:
    image: redis:7-alpine
    container_name: talentup-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    expose:
      - "6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  backend_uploads:
  redis_data:
  caddy_data:
  caddy_config:
